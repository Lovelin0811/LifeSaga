<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lovelin.lifesaga.saga.infrastructure.persistence.mapper.SagaNodeFavoriteMapper">

    <select id="countByUserIdAndNodeId" resultType="int">
        SELECT COUNT(*)
        FROM node_favorites
        WHERE user_id = #{userId}
          AND node_id = #{nodeId}
    </select>

    <insert id="insert">
        INSERT INTO node_favorites (user_id, node_id)
        VALUES (#{userId}, #{nodeId})
    </insert>

    <insert id="insertIgnore">
        INSERT IGNORE INTO node_favorites (user_id, node_id)
        VALUES (#{userId}, #{nodeId})
    </insert>

    <delete id="deleteByUserIdAndNodeId">
        DELETE FROM node_favorites
        WHERE user_id = #{userId}
          AND node_id = #{nodeId}
    </delete>

    <select id="findNodeIdsByUserIdAndNodeIds" resultType="long">
        SELECT node_id
        FROM node_favorites
        WHERE user_id = #{userId}
          AND node_id IN
        <foreach collection="nodeIds" item="nodeId" open="(" separator="," close=")">
            #{nodeId}
        </foreach>
    </select>

    <delete id="deleteByNodeId">
        DELETE FROM node_favorites
        WHERE node_id = #{nodeId}
    </delete>

    <delete id="deleteBySagaId">
        DELETE nf
        FROM node_favorites nf
        JOIN saga_nodes sn ON nf.node_id = sn.id
        WHERE sn.saga_id = #{sagaId}
    </delete>

</mapper>
