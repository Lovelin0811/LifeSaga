# LifeSaga 待开发功能清单

> 本文档记录已规划但尚未实现的功能，按优先级排列。

---

## P0 — 核心体验缺失（必须做）

| # | 功能 | 现状 | 需要做什么 |
|---|------|------|-----------|
| 1 | **广场页（discover）** | 完全空壳，只显示一句静态文案「探索其他冒险者的副本」 | ① 数据库 sagas 表加 `is_public` 字段；② 后端加 `GET /api/sagas/public` 公开副本列表 API；③ 前端 discover 页面实现卡片流 + 下拉刷新 |

---

## P1 — 功能链断裂

| # | 功能 | 现状 | 需要做什么 |
|---|------|------|-----------|
| 2 | **完成副本** | saga 创建后 status 永远是 active，用户无法标记副本为「已完成」 | 后端加 `PUT /api/sagas/{id}/complete` 接口，设置 `status=completed` + `endedAt=now()` |
| 3 | **编辑昵称 / 头像** | profile 页面只展示，无编辑入口。后端 API 已就绪但从未被调用 | profile 页面加头像、昵称点击编辑交互 |
| 4 | **首页搜索** | 搜索框点击只弹 toast「搜索功能开发中」 | 后端加 `GET /api/sagas?keyword=` 搜索接口，前端实现搜索下拉 |

---

## P2 — 页面功能占位

| # | 功能 | 现状 | 需要做什么 |
|---|------|------|-----------|
| 5 | **节点收藏** | node-detail 收藏按钮只弹 toast | 数据库加收藏表 → 后端加 toggle 接口 → 前端收藏交互 |
| 6 | **节点里程碑切换** | 创建时可选，但 node-detail 的切换按钮显示「开发中」 | 后端 `PUT /api/sagas/{sagaId}/nodes/{id}/toggle-milestone` |
| 7 | **节点分享** | 分享按钮弹 toast「分享功能开发中」 | 微信小程序分享卡片 + 生成节点海报图 |
| 8 | **我的相册** | profile 菜单点击弹 toast | 聚合所有节点的照片，按时间/副本分组展示 |
| 9 | **数据统计** | profile 菜单点击弹 toast | 副本数/节点数/成就数趋势图、时间热力图等 |
| 10 | **提醒设置** | profile 菜单点击弹 toast | 定时推送提醒添加节点 |
| 11 | **主题与外观** | profile 菜单点击弹 toast | 浅色/深色主题切换 |

---

## P3 — 数据层完善

| # | 问题 | 具体表现 | 需要做什么 |
|---|------|---------|-----------|
| 12 | `Saga.startedAt` 创建时未赋值 | 前端读取 `startedAt` 但永远为空 | SagaService.create() 加 `setStartedAt(LocalDateTime.now())` |
| 13 | `Saga.endedAt` 完全未使用 | 数据库字段存在但全代码无赋值 | 配合「完成副本」功能，完成时设置 endedAt |
| 14 | `SagaNode.sortOrder` 从未使用 | 节点排序完全按创建时间，用户无法调整顺序 | 支持拖拽排序或手动设序号 |
| 15 | 成就详情页缺失 | 点击成就卡片无任何反应 | 新建成就详情页，展示条件、奖励、解锁状态 |
| 16 | mythic 稀有度缺少前端样式 | 30+ 节点副本无对应 CSS 边框效果 | achievements.wxss 补 `.badge-card--mythic` 样式 |

---

## 附录：当前开发中的提示

以下 toast 提示分布在代码中，触及时会告知用户「开发中」：

| 页面 | 触发操作 | toast 文案 |
|------|---------|-----------|
| home | 点击搜索框 | 搜索功能开发中 |
| profile | 点击「我的相册」 | 我的相册 - 开发中 |
| profile | 点击「数据统计」 | 数据统计 - 开发中 |
| profile | 点击「提醒设置」 | 提醒设置 - 开发中 |
| profile | 点击「主题与外观」 | 主题与外观 - 开发中 |
| node-detail | 点击收藏 | 收藏功能开发中 |
| node-detail | 点击里程碑 | 里程碑功能开发中 |
| node-detail | 点击分享 | 分享功能开发中 |
