# LifeSaga 待开发功能清单

> 本文档记录已规划但尚未实现的功能，按优先级排列。

---

## P0 — 核心体验缺失（必须做）

| # | 功能 | 现状 | 需要做什么 |
|---|------|------|-----------|
| 1 | **广场页（discover）** | 已实现：`is_public` 公开副本 + `GET /api/sagas/public` + discover 卡片流与搜索 | — |

---

## P1 — 功能链断裂

| # | 功能 | 现状 | 需要做什么 |
|---|------|------|-----------|
| 2 | **完成副本** | 已实现：`PUT /api/sagas/{id}/complete` 接口，完成时设置 `status=completed` + `endedAt=now()` | — |
| 3 | **编辑昵称 / 头像** | 已实现：profile 页面支持头像、昵称编辑并保存 | — |
| 4 | **首页搜索** | 已实现：`GET /api/sagas?keyword=` + 首页输入搜索 | — |

---

## P2 — 页面功能占位

| # | 功能 | 现状 | 需要做什么 |
|---|------|------|-----------|
| 5 | **节点收藏** | 已实现：node-detail 收藏按钮可切换收藏状态 | — |
| 6 | **节点里程碑切换** | 已实现：node-detail 可切换里程碑状态 | — |
| 7 | **节点分享** | 已实现：原生分享卡片与分享入口 | — |
| 8 | **我的相册** | 已实现：按节点照片聚合展示 | — |
| 9 | **数据统计** | 已实现：副本/节点/完成/公开统计页 | — |
| 10 | **提醒设置** | 已实现：本地提醒开关状态保存 | — |
| 11 | **主题与外观** | 已实现：浅色/深色主题切换并保存 | — |

---

## P3 — 数据层完善

| # | 问题 | 具体表现 | 需要做什么 |
|---|------|---------|-----------|
| 12 | `Saga.startedAt` 创建时未赋值 | 已实现：创建副本时自动设置 `startedAt` | — |
| 13 | `Saga.endedAt` 完全未使用 | 已实现：完成副本时设置 endedAt | — |
| 14 | `SagaNode.sortOrder` 从未使用 | 已实现：添加/编辑节点时可手动设置排序编号 | — |
| 15 | 成就详情页缺失 | 已实现：点击成就卡片可查看详情弹窗 | — |
| 16 | mythic 稀有度缺少前端样式 | 已实现：补了 `.badge-card--mythic` 样式 | — |

---

## 附录：当前开发中的提示

这部分已清空，代码里不再保留“开发中”占位提示。
